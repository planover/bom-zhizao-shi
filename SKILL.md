---
name: bom-zhizao-shi
description: BOM智造师 —— 交互式引导用户录入物料信息与工艺工序信息，自动完成数据校验并生成标准 BOM 表 Excel 文件。当用户提及「BOM生成」「物料清单制作」「BOM表」「做一份物料清单」「生成工艺BOM」等关键词，或希望把物料（名称/单位/用量/出品率）与工序（编号/名称/说明/工时/备注）整理成结构化 Excel 时，使用此技能。
---

# BOM智造师（BOM Maker）

## 概述
本技能通过分两阶段的交互式引导，采集「物料信息」与「工艺工序信息」，在完成数据校验与汇总确认后，调用 `scripts/generate_bom.py` 生成带表头、物料区、工序区的 BOM 表 Excel 文件（.xlsx）。

适用场景：
- 用户说「帮我生成一份 BOM 表」「做物料清单」「BOM 生成」
- 需要把产品物料（名称 / 单位 / 用量 / 出品率）与工序（编号 / 名称 / 说明 / 工时 / 备注）整理成标准 Excel
- 制造、工艺、采购、成本核算等需要结构化 BOM 的场景

## 触发与入口
满足以下任一即触发：
- 关键词命中：`BOM生成`、`物料清单制作`、`BOM表`、`物料清单`、`工艺BOM`、`生成BOM`
- 用户意图明确为「把物料和工序做成 Excel 表格」

触发后，直接进入「第一阶段：物料信息采集」，不要先做其他事。

## 执行链路总览
```
触发 → 阶段一(物料采集) → 阶段二(工序采集) → 数据校验 → 汇总确认 → 生成Excel → 输出
                 ↑ 任意阶段可取消(重新输入 / 退出)
```
每一步的跳转条件与异常处理见后文对应小节。

## 第一阶段：物料信息采集
目标：采集至少一条物料记录（可多条，构成「物料清单」）。每条物料含 4 个字段：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 物料名称 | 物料的中文 / 英文名称 | 是 | 非空 |
| 计量单位 | 如 kg、个、米、升 | 是 | 非空 |
| 用量 | 单件产品消耗该物料的数量 | 是 | 正数（>0），数值 |
| 出品率 | 该物料产出成品的比率（%） | 是 | 正数（>0）且 ≤100，数值 |

引导方式（每轮聚焦一条，降低认知负担）：
1. 输出物料录入模板，例如：
   ```
   请填写一条物料（可按格式整段回复，也可逐字段回复）：
   物料名称：
   计量单位：
   用量：
   出品率(%)：
   ```
2. 解析用户输入，逐项校验（见「数据校验」）。
3. 校验失败时，明确指出哪个字段、何种问题，要求「重新输入该项」；校验通过后存入临时物料列表。
4. 询问「是否继续添加物料？(y/n）」。
   - `y` → 回到步骤 1 采集下一条
   - `n` → 物料数 ≥1 则进入第二阶段；若 0 条则提示「至少需要一条物料」并返回步骤 1
5. 支持批量：若用户一次性给出多条物料，按空行 / 序号拆分后逐条校验。

## 第二阶段：工艺工序采集
目标：采集零条或多条工序记录（允许无工序的纯物料 BOM）。每条工序含 5 个字段：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 工序编号 | 如 S01、1、OP-10 | 是 | 非空，建议连续，不可重复 |
| 工序名称 | 如 切割、焊接、装配 | 是 | 非空 |
| 工序说明 | 操作要点 | 否 | 文本 |
| 工时 | 单件标准工时（可带单位，如 15min） | 否 | 若为数值须 ≥0 |
| 备注 | 其他说明 | 否 | 文本 |

引导方式：
1. 首次进入时说明：「接下来采集工艺工序，可添加多道；全部完成后回复『结束』即可」。
2. 输出工序录入模板，工序编号可自动建议（上一条 +1）：
   ```
   工序编号（默认 S01）：
   工序名称：
   工序说明：
   工时：
   备注：
   ```
3. 解析并校验（工时若填数值须 ≥0；必填项缺失则要求补充；编号重复则要求修改）。
4. 询问「继续添加下一道工序？(y/n 或 回复『结束』）」。
   - 继续 → 回到步骤 2
   - 结束 / `n` → 进入「数据校验 → 汇总确认」
5. 允许 0 道工序：用户直接「结束」即视为纯物料 BOM。

## 数据校验
在每字段录入后即时校验，并在汇总前整体复检：
- 必填字段缺失 → 提示「【字段名】为必填，请补充」，停留当前项重新输入。
- 用量非数值或 ≤0 → 提示「用量必须为正数，请重新输入」。
- 出品率非数值、≤0 或 >100 → 提示「出品率须为 0–100 之间的正数(%)」。
- 工时若填数值须 ≥0；非数值且非空 → 提示格式异常，要求重新输入或留空。
- 工序编号重复 → 提示「工序编号 X 已存在，请修改」。
- 格式异常（如用量填了文字、单位含非法字符） → 明确指出并重试，不限次数直到合法。

校验全部通过才允许进入汇总确认。

## 汇总确认
采集完成后，输出一段结构化汇总供用户确认：
```
===== BOM 信息汇总（请确认）=====
版本号：V1.0   生成日期：2026-07-07
【物料】（共 N 条）
 1. 名称 / 单位 / 用量 / 出品率
 ...
【工序】（共 M 条）
 1. 编号 / 名称 / 说明 / 工时 / 备注
 ...
================================
确认生成？(y 生成 / 修改 重新输入 / 退出)
```
- `y` → 生成 Excel
- `修改` → 进入「修改模式」：让用户指出改哪一项（物料第几条 / 工序第几条 / 版本号），回到对应采集步骤重新录入该项，然后重新汇总。
- `退出` → 按「取消与退出」处理。

版本号默认 `V1.0`，生成日期默认当天（YYYY-MM-DD）；若用户指定则采用用户给定值。

## 生成 Excel
确认后执行：
1. 将汇总数据序列化为 JSON（结构见 `references/bom-spec.md` 的「输入 JSON Schema」）。
2. 调用生成脚本（脚本会自动确保 openpyxl 可用，无需预先配置）：
   ```
   python3 <skill_dir>/scripts/generate_bom.py --data <临时json文件> --out <输出路径.xlsx>
   ```
   说明：`<skill_dir>` 为本技能所在目录；输出路径默认当前工作目录，文件名 `BOM_<日期>.xlsx`，用户可指定。
3. 脚本打印 `OK:<路径>` 表示成功，打印 `VALIDATION_FAILED` 及错误列表表示数据问题——此时回到「数据校验」修正后重试。
4. 将生成的 .xlsx 绝对路径作为结果呈现给用户。

## 取消与退出
用户在任意阶段输入「取消 / 退出 / 不做了 / exit / quit」等中止意图时：
- 提示并提供选项：
  ```
  已取消本次录入。请选择：
  [1] 重新输入（从当前阶段该项重新开始，已采集的合法数据保留）
  [2] 退出（放弃本次 BOM 生成，不生成任何文件）
  ```
- 选 1 → 回到触发取消的那一项，重新引导录入。
- 选 2 → 终止流程，明确告知「未生成任何文件」，并提示可随时说「BOM生成」重新发起。

## 异常处理速查
| 情况 | 处理 |
|------|------|
| 用量 / 出品率格式非法 | 指出字段，要求重新输入该项 |
| 必填缺失 | 提示补充，停留当前项 |
| 工序编号重复 | 提示修改 |
| 用户中途取消 | 见「取消与退出」 |
| openpyxl 缺失 | 脚本自动 `pip install openpyxl`；仍失败则提示手动安装 |
| 输出路径不可写 | 改默认路径（当前目录）并重试，提示用户 |
| 输出路径已存在同名文件 | 追加序号（如 `BOM_2026-07-07_2.xlsx`）或询问是否覆盖 |

## Resources
### scripts/
- `generate_bom.py`：读取 JSON 数据，生成 BOM 表 .xlsx（标题「BOM表」、版本号、生成日期、物料区、工序区），含样式与防御性校验；依赖 openpyxl，缺失时自装。

### references/
- `bom-spec.md`：BOM 表 Excel 结构规范与输入 JSON Schema，供生成脚本与汇总序列化参考。
