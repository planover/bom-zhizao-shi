---
name: bom-zhizao-shi
description: BOM智造师 —— 交互式引导用户录入物料信息与工艺工序信息，自动完成数据校验并生成标准 BOM 表 Excel 文件；同时支持从已有 BOM 表 Excel 逆向导入为结构化 JSON。当用户提及「BOM生成」「物料清单制作」「BOM表」「做一份物料清单」「生成工艺BOM」等正向关键词，或提及「BOM导入」「从Excel导入BOM」「反向导入BOM」「导入BOM表」「BOM表导入」等逆向关键词，或希望把物料（名称/单位/用量/出品率）与工序（编号/名称/说明/工时/备注）整理成结构化 Excel / 解析已有 BOM 表时，使用此技能。
---

# BOM智造师（BOM Maker）

## 概述
本技能通过分阶段的交互式引导，采集「产品信息」「物料信息」与「工艺工序信息」，在完成数据校验与汇总确认后，调用 `scripts/generate_bom.py` 生成带表头、产品名称、物料区（含 ERP 物料代码）、工序区的 BOM 表 Excel 文件（.xlsx）。V4 新增行业标识（`industry`），按行业自动派生专属视图（食品→配料表、电子→元件清单、化工→配方表）。

适用场景：
- 用户说「帮我生成一份 BOM 表」「做物料清单」「BOM 生成」
- 需要把产品物料（名称 / 单位 / 用量 / 出品率 / ERP物料代码）与工序（编号 / 名称 / 说明 / 工时 / 备注）整理成标准 Excel
- 电子行业需生成含位号/型号/封装/RoHS 的元件清单
- 化工行业需生成含 CAS号/含量(%)/GHS标识 的配方表
- 制造、工艺、采购、成本核算等需要结构化 BOM 的场景

## 触发与入口
满足以下任一即触发：

**正向（生成 BOM）**：
- 关键词命中：`BOM生成`、`物料清单制作`、`BOM表`、`物料清单`、`工艺BOM`、`生成BOM`
- 用户意图明确为「把物料和工序做成 Excel 表格」

**逆向（从 Excel 导入 BOM）**：
- 关键词命中：`BOM导入`、`从Excel导入BOM`、`反向导入BOM`、`导入BOM表`、`BOM表导入`
- 用户意图明确为「把已有 BOM 表 .xlsx 解析回结构化数据 / 重新编辑已有 BOM」

触发后，先进入「第零阶段：产品信息采集」，再进入「第一阶段：物料信息采集」，不要先做其他事。

## 执行链路总览
```
【正向】触发 → 阶段零(产品信息) → 阶段一(物料采集) → 阶段二(工序采集) → 数据校验 → 汇总确认 → 生成Excel → 输出
                 ↑ 任意阶段可取消(重新输入 / 退出)

【逆向】触发(导入类关键词) → 接收Excel路径 → 逆向解析(import_bom.py) → 展示/导出JSON → [重新生成Excel / 退出]
```
每一步的跳转条件与异常处理见后文对应小节。

## 第零阶段：产品信息采集
目标：采集产品级信息，包括产品名称（必填）、产品类别（必填）、全产品出品率（必填）、行业标识（选填）、版本号与生成日期。

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 产品名称 | 产品的中 / 英文名称（如：芒果果味糖浆） | **是** | 非空（R1），留空将触发 VALIDATION_FAILED |
| 产品类别 | 产品分类 | **是** | 下拉 5 类：`食品`/`工业品`/`日化化妆品`/`医药`/`其他`（R1/R4） |
| 全产品出品率 | 全产品出品率(%)（如干香菇泡发增重可 >100，通常 ≤100） | **是** | 数值 `> 0`，允许 `> 100`（无硬上限）（R2） |
| 行业标识 | 产品所属行业，决定专属派生视图 | 否 | 下拉 8 类：`食品`/`电子`/`化工`/`机械`/`纺织`/`家具`/`包装`/`通用`；默认按 `category` 推断（食品→食品，日化/医药→化工，工业品/其他→通用）；非法值 V8 WARNING 回退推断；直接回车跳过即用推断值 |
| 版本号 | BOM 版本号 | 否 | 默认 `V1.0` |
| 生成日期 | BOM 生成日期 | 否 | 默认当天（YYYY-MM-DD） |
| 审批人 | BOM 审批人（可选） | 否 | 默认 `""`；非空才在表头行 5 显示 |
| 生效日期 | BOM 生效日期（可选） | 否 | 默认 `""`；非空才在表头行 5 显示 |
| 执行标准 | BOM 执行标准（可选） | 否 | 默认 `""`；行业建议：食品→`GB 7718-2025`，电子→`GB/T 39560`，化工→`GB/T 16483-2008`，纺织→`FZ/T 80004`，家具→`QB/T 1951.1`；非空才在表头行 5 显示 |

引导方式：
1. 询问用户：「请输入产品名称（如：芒果果味糖浆）」（必填，不可留空）。
   - 用户输入 → 记录 `product_name`
   - 用户留空 → 提示「产品名称为必填，请补充」，停留重新输入
2. 询问产品类别：「请选择产品类别：食品 / 工业品 / 日化化妆品 / 医药 / 其他」（必填，建议以下拉呈现）。记录 `category`。
3. 询问全产品出品率：「请输入全产品出品率(%)（必填，>0，可>100，如干香菇泡发增重）」。记录 `output_rate`（数值，校验 >0）。
4. 询问行业标识（选填）：「请选择行业（可选，直接回车跳过即自动推断）：食品 / 电子 / 化工 / 机械 / 纺织 / 家具 / 包装 / 通用」。
   - 用户选择 → 记录 `industry`（V8 校验枚举合法性）
   - 用户回车跳过 → 按 `category` 推断（食品→食品，日化/医药→化工，工业品/其他→通用），不打印告警
   - 用户输入非法值 → V8 WARNING「industry 值『{value}』不在枚举内，已回退为推断值」（非阻断，退出码 0）
   - 行业决定专属派生视图：食品→配料表，电子→元件清单（14 列扩列，含 RoHS 着色），化工→配方表（13 列扩列），纺织→面料辅料清单，家具→家具物料清单，机械→机械物料清单（8 列，含图号/材质/热处理/表面处理/重量/单重），包装→包装物料清单（8 列，含物料类型/材质/克重/尺寸/印刷工艺/环保标识）；通用→无行业专属视图（成本明细视图跨行业按需生成）
5. 可同时确认版本号（默认 `V1.0`）与生成日期（默认当天）；也可留到汇总确认时再确认。
6. 可选采集「审批人 / 生效日期 / 执行标准」（均为选填，留空则不显示）。三项非空时拼接显示于表头行 5（段间以空格分隔）；执行标准按行业给出建议值（食品→GB 7718-2025，电子→GB/T 39560，化工→GB/T 16483-2008，纺织→FZ/T 80004，家具→QB/T 1951.1，机械→GB/T 1804-2000，包装→GB/T 6543-2008；均可改）。
7. 采集后进入「第一阶段：物料信息采集」。

> 说明：食品类预览「配料表」（提示排除的包材/其他类）；电子类预览「元件清单」（14 列，含 RoHS 着色与 W2 告警、排除"其他"类）；化工类预览「配方表」（13 列，含 CAS/GHS 的 W3 告警、含量和校验、排除"包材"类）；纺织类预览「面料辅料清单」（排除"其他"类）；家具类预览「家具物料清单」（排除"其他"类）；机械类预览「机械物料清单」（排除"其他"类，图号/材质/热处理/表面处理/重量/单重）；包装类预览「包装物料清单」（排除"其他"类，材质/克重/尺寸/印刷工艺/环保标识）；任一物料填了 `unit_price` 还会预览跨行业「成本明细」（单价/币种/总价=用量×单价，机械/包装带成本时为「四、成本明细」）。V5/V6 不新增任何阻断/软校验，仅复用 V4 同构的排除提示。

## 第一阶段：物料信息采集
目标：采集至少一条物料记录（可多条，构成「物料清单」）。每条物料含 7 个基础字段，另按 `industry` 动态追加行业专属字段：

### 基础字段（所有行业）

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 物料名称 | 物料的中文 / 英文名称 | 是 | 非空 |
| 计量单位 | 如 kg、个、米、升 | 是 | 非空 |
| 用量 | 单件产品消耗该物料的数量 | 是 | 正数（>0），数值 |
| 出品率 | 该物料产出成品的比率（%） | 是 | 正数（>0）且 ≤100，数值 |
| ERP物料代码 | 物料在 ERP 系统中的编码 | 否 | 无，可留空 |
| 物料类型 | 物料分类（用于专属视图过滤） | 否 | 默认 `其他`；食品：原料/添加剂/香精香料/包材/其他；电子：电阻/电容/IC/连接器/二极管/三极管/晶振/其他；化工：主料/溶剂/催化剂/添加剂/包材/其他；纺织：面料/辅料/纱线/印染/五金/其他；家具：主材/板材/辅材/五金/面料/其他；**机械：零部件/标准件/型材/铸件/焊接件/其他；包装：纸箱/缓冲/标签/胶带/薄膜/其他** |
| 所属工序 | 该物料归属的工序（工序级流转链/分工序呈现） | 否 | 下拉引用已录入工序 `step_no`，首道工序可选填，默认空 |
| 过敏原 | 该物料的过敏原标签（仅食品类在配料表展示） | 否 | 逗号分隔，∈ 八大类+其他；默认空；标签非法仅 WARNING 不阻断 |

### 行业专属字段（V4 新增，V5 扩充；按 industry 动态追加）

> **模板驱动（V5 新增 `INDUSTRY_TEMPLATES`）**：阶段一物料模板、物料类型下拉、执行标准预填、一键载入工序，均按 `INDUSTRY_TEMPLATES[industry]` 动态生成，交互层只读此模板，不写入任何新的 JSON 结构字段（向后兼容旧 JSON）。
> - `material_types`：物料类型下拉建议值（非强制枚举）；
> - `standard`：执行标准自动预填（可改，见「执行标准」行）；
> - `special_fields`：阶段一模板动态追加的专属字段行（见下表，按行业列出）；
> - `preset_processes`：可选「一键载入工序模板」（仅引导，不写新结构）。
>
> **软校验约定（V5 不变）**：V5 **不新增任何阻断/软校验**。仅沿用 V4 既有 W2（电子 RoHS 未标）、W3（化工 CAS/GHS 未填）与含量(%) 列和校验；纺织/家具/成本专属字段均不设软校验。

**电子行业（industry=="电子"）**——元件清单专属（`三、元件清单`，V5 扩列至 14 列 A–N）：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 位号(designator) | 电子元件位号（如 R1、C3、U5） | 否 | 默认 `""`；空则元件清单排同类型末尾 |
| 型号(part_number) | 电子元件型号（如 STM32F103C8T6） | 否 | 默认 `""` |
| 封装(footprint) | 电子元件封装（如 0805、SOIC-8） | 否 | 默认 `""` |
| RoHS(rohs) | RoHS 合规状态 | 否 | ∈ {是,否,未知}；默认 `""`（等价未知）；空则触发 **W2 软告警**（沿用 V4）；否→红字，未知/空→黄字 |
| 制造商(manufacturer) | 元件制造商（如 ST、TI） | 否 | 默认 `""` |
| 容差(tolerance) | 标称容差（如 ±1%、±5%） | 否 | 默认 `""` |
| 额定功率(rated_power) | 额定功率（如 0.25W） | 否 | 默认 `""` |
| 额定电压(rated_voltage) | 额定电压（如 50V） | 否 | 默认 `""` |
| 替代料(alternate) | 可替代型号（pin-to-pin） | 否 | 默认 `""` |
| 封装温度(reflow_temp) | 回流焊峰值温度（如 260℃） | 否 | 默认 `""` |

**化工行业（industry=="化工"）**——配方表专属（`三、配方表`，V5 扩列至 13 列 A–M）：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| CAS号(cas_number) | 化学品 CAS 登记号（如 7732-18-5） | 否 | 默认 `""`；空则触发 **W3 软告警**（沿用 V4） |
| 含量(%)（concentration） | 配方中该原料的含量百分比 | 否 | 默认 `""`；数值；空则留空不显示 0.0%；列和校验 ±5%（沿用 V4） |
| GHS标识(ghs_hazard) | GHS 危险标识（如 GHS07） | 否 | 默认 `""`；空则触发 **W3 软告警**（沿用 V4） |
| 纯度(purity) | SDS 纯度（如 ≥95%） | 否 | 默认 `""` |
| 物态(physical_state) | 物态（固/液/气） | 否 | 默认 `""` |
| 闪点(flash_point) | 闪点（如 13℃） | 否 | 默认 `""` |
| 存储条件(storage_condition) | 存储条件（如 阴凉干燥） | 否 | 默认 `""` |
| 危险等级(hazard_class) | 危险类别（如 易燃液体 类别2） | 否 | 默认 `""` |

**纺织行业（industry=="纺织"）**——面料辅料清单专属（`三、面料辅料清单`，8 列 A–H）：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 成分比例(composition) | 成分及比例（如 棉60%/涤40%） | 否 | 默认 `""` |
| 纱支(yarn_count) | 纱支（如 40S、21S×21S） | 否 | 默认 `""` |
| 克重(g/m²)（fabric_weight） | 单位面积质量 | 否 | 默认 `""`；数值 |
| 幅宽(width) | 幅宽（如 150cm） | 否 | 默认 `""` |
| 色号(color_no) | 色号（如 藏青 01） | 否 | 默认 `""` |

**家具行业（industry=="家具"）**——家具物料清单专属（`三、家具物料清单`，8 列 A–H）：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 材质等级(material_grade) | 材质等级（如实木/板木/金属） | 否 | 默认 `""` |
| 尺寸规格(spec_size) | 尺寸规格（如 1200×600×750mm） | 否 | 默认 `""` |
| 表面处理(surface_treatment) | 表面处理（如 烤漆/贴皮） | 否 | 默认 `""` |
| 色号/花色(color_no) | 色号或花色（如 胡桃色） | 否 | 默认 `""` |

**机械行业（industry=="机械"）**——机械物料清单专属（`三、机械物料清单`，8 列 A–H，V6 新增；**不单列「物料类型」展示列**，物料类型仅用于过滤/排序）：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 图号(drawing_no) | 零部件图纸编号（如 DW-2026-011） | 否 | 默认 `""` |
| 材质(material) | 材质（如 Q235、45#钢、铝合金6061） | 否 | 默认 `""` |
| 热处理(heat_treatment) | 热处理工艺（如 退火、调质、淬火+回火） | 否 | 默认 `""` |
| 表面处理(surface_treatment) | 表面处理（如 镀锌、喷塑、阳极氧化，与家具同名 key） | 否 | 默认 `""` |
| 重量(weight) | 单件总重量（kg） | 否 | 默认 `""`；数值 |
| 单重(unit_weight) | 单位重量（kg/件） | 否 | 默认 `""`；数值；与 weight 保留不合并（主理人拍板 Q1） |

**包装行业（industry=="包装"）**——包装物料清单专属（`三、包装物料清单`，8 列 A–H，V6 新增；保留「物料类型」展示列）：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 材质(material) | 材质（如 瓦楞纸、PET、BOPP，与机械同名 key） | 否 | 默认 `""` |
| 克重(basis_weight) | 纸张/薄膜克重（g/m²） | 否 | 默认 `""`；数值 |
| 尺寸(size) | 尺寸（如 400×300×200mm） | 否 | 默认 `""` |
| 印刷工艺(print_process) | 印刷工艺（如 胶印、柔印、数码印刷） | 否 | 默认 `""` |
| 环保标识(eco_label) | 环保标识（如 FSC、可回收、可降解） | 否 | 默认 `""`；自由文本，无受限枚举（主理人拍板 Q5） |

**成本字段（跨行业，任一物料填 `unit_price` 即纳入 `成本明细`）**：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 单价(unit_price) | 物料单位价格（数值） | 否 | 默认 `""`（不填则不进成本视图）；`total_price` 不入库，按 `用量×单价` 实时派生 |
| 币种(currency) | 价格币种 | 否 | 默认 `人民币(CNY)`；缺省即按人民币(CNY)处理 |

> 注：通用/机械/包装行业不生成「三、」行业专属视图，仅当物料含 `unit_price` 时生成「三、成本明细」。

引导方式（每轮聚焦一条，降低认知负担）：
1. 输出物料录入模板（按 `INDUSTRY_TEMPLATES[industry].special_fields` 动态追加专属字段行），例如：
   ```
   请填写一条物料（可按格式整段回复，也可逐字段回复）：
   物料名称：
   计量单位：
   用量：
   出品率(%)：
   ERP物料代码：（可选，可留空）
   物料类型：（可选，下拉值见本行业模板 material_types）
   所属工序：（可选，引用工序编号 step_no，首道可留空）
   过敏原：（可选，仅食品；逗号分隔标签）
   --- 以下为行业专属字段（按行业动态展示，可选）---
   位号(designator)：           # 电子
   型号(part_number)：          # 电子
   封装(footprint)：            # 电子
   RoHS(rohs)：                 # 电子；是/否/未知，留空等价未知
   制造商(manufacturer)：       # 电子
   容差(tolerance)：            # 电子
   额定功率(rated_power)：      # 电子
   额定电压(rated_voltage)：    # 电子
   替代料(alternate)：          # 电子
   封装温度(reflow_temp)：      # 电子
   --- 或 ---
   CAS号(cas_number)：          # 化工
   含量(%)（concentration）：   # 化工
   GHS标识(ghs_hazard)：        # 化工
   纯度(purity)：               # 化工
   物态(physical_state)：       # 化工
   闪点(flash_point)：          # 化工
   存储条件(storage_condition)：# 化工
   危险等级(hazard_class)：     # 化工
   --- 或 ---
   成分比例(composition)：      # 纺织
   纱支(yarn_count)：           # 纺织
   克重(g/m²)（fabric_weight）：# 纺织
   幅宽(width)：                # 纺织
   色号(color_no)：             # 纺织
   --- 或 ---
   材质等级(material_grade)：   # 家具
   尺寸规格(spec_size)：        # 家具
   表面处理(surface_treatment)：# 家具
   色号/花色(color_no)：        # 家具
   --- 成本字段（跨行业，可选）---
   单价(unit_price)：（数值；填写即纳入成本明细）
   币种(currency)：（缺省 人民币(CNY)）
   ```
   > 仅展示与当前 `industry` 匹配的专属字段行（按 `special_fields` 渲染）；通用/机械/包装不展示行业专属字段行。
   > 首次进入阶段一、或切换 industry 时，提示「本行业提供 N 条工序模板，是否一键载入？(y/n）」；`y` 则按 `preset_processes` 预填第二阶段工序（仅引导，不写新 JSON 结构）。
2. 解析用户输入，逐项校验（见「数据校验」）。
3. 校验失败时，明确指出哪个字段、何种问题，要求「重新输入该项」；校验通过后存入临时物料列表。
4. 询问「是否继续添加物料？(y/n）」。
   - `y` → 回到步骤 1 采集下一条
   - `n` → 物料数 ≥1 则进入第二阶段；若 0 条则提示「至少需要一条物料」并返回步骤 1
5. 支持批量：若用户一次性给出多条物料，按空行 / 序号拆分后逐条校验。

## 第二阶段：工艺工序采集
目标：采集零条或多条工序记录（允许无工序的纯物料 BOM）。每条工序含 6 个字段：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 工序编号 | 如 S01、1、OP-10 | 是 | 非空，建议连续，不可重复 |
| 工序名称 | 如 切割、焊接、装配 | 是 | 非空 |
| 工序说明 | 操作要点 | 否 | 文本 |
| 工时 | 单件标准工时（可带单位，如 15min） | 否 | 若为数值须 ≥0 |
| 备注 | 其他说明 | 否 | 文本 |
| 产物(output) | 该工序产出的半成品/成品名称（工序流转链 R3 源头） | **是** | 非空；须作为下一道工序（按 step_no 升序）物料清单中的一条物料名称（R3） |

引导方式：
1. 首次进入时说明：「接下来采集工艺工序，可添加多道；全部完成后回复『结束』即可」。
2. 输出工序录入模板，工序编号可自动建议（上一条 +1）：
   ```
   工序编号（默认 S01）：
   工序名称：
   工序说明：
   工时：
   备注：
   产物(output)：（必填，该工序产物名称，需作为下道工序的物料之一）
   ```
3. 解析并校验（工时若填数值须 ≥0；必填项缺失则要求补充；编号重复则要求修改；`output` 必填非空）。
4. 多道工序时，提醒用户：下一道工序的物料清单中必须包含上一道工序的 `产物`（名称精确匹配），否则汇总/生成阶段将报「流转链不完整」（R3，阻断级）。
5. 询问「继续添加下一道工序？(y/n 或 回复『结束』）」。
   - 继续 → 回到步骤 2
   - 结束 / `n` → 进入「数据校验 → 汇总确认」
6. 允许 0 道工序：用户直接「结束」即视为纯物料 BOM（此时不触发 R3 流转链校验）。

## 数据校验
在每字段录入后即时校验，并在汇总前整体复检（校验规则 V1–V7 + V8，与 `generate_bom.py` 完全一致）：

**产品级（V1/V2/V3）**
- V1(R1)：`product_name` 必填非空 → 提示「产品名称为必填，且不可为空」。
- V2(R1)：`category` 必填且 ∈ {食品,工业品,日化化妆品,医药,其他} → 提示「产品类别为必填，且须为：食品/工业品/日化化妆品/医药/其他」。
- V3(R2)：`output_rate` 必填且 >0（允许 >100）→ 提示「全产品出品率(output_rate)为必填，且须为正数（可大于100，如干香菇泡发增重）」。

**物料级（V4/V7）**
- V4(R2)：`yield_rate` 须 0<值≤100 → 提示「物料#{i} 出品率须为 0-100 的正数」。
- 用量非数值或 ≤0 → 提示「用量必须为正数，请重新输入」。
- 物料名称/计量单位非空校验；格式异常（如用量填了文字） → 明确指出并重试。

**工序级（V5/V6）**
- V5(R3 流转链，阻断级)：仅当工序数 ≥2 时，上一工序 `output` 必填（否则提示「工序 {Sxx} 的产物(output)为必填」）；且下一道工序的物料清单（按 `process == 该工序.step_no` 过滤）必须包含上一工序 `output` 作为一条物料（名称精确匹配），否则提示「工序 {Syy} 的物料清单未包含上一工序 {Sxx} 的产物『{output}』，流转链不完整」。
- V6：工序编号唯一、名称非空、`work_hours` 若填数值须 ≥0；编号重复 → 提示「工序编号 X 已存在，请修改」。

**行业级（V8 软校验，非阻断）**
- V8：`industry` 非空但不在 8 值枚举内（食品/电子/化工/机械/纺织/家具/包装/通用）→ 打印 `WARNING: industry 值『{value}』不在枚举内（食品/电子/化工/机械/纺织/家具/包装/通用），已回退为推断值`，**不阻断**生成，退出码 0。

**物料级（W1 软告警，非阻断）**
- W1：食品类物料的 `allergen` 若填写，其逗号分隔的每一标签须 ∈ 八大类+其他集合（含麸质谷物/甲壳类/蛋类/鱼类/花生/大豆/乳/坚果/其他）；出现非法标签仅打印 `WARNING: 物料『{name}』的过敏原标签『{tag}』不在八大类集合（…）内，请确认`，**不阻断**生成，可确认后继续。
- H1（名称关键词启发式，非阻断）：若物料**名称**含致敏物关键词（如 牛奶→乳、蛋→蛋类、花生→花生、大豆/黄豆→大豆、麸质/面筋→含麸质谷物、坚果→坚果、虾/蟹→甲壳类、鱼→鱼类 等）而其 `allergen` 未涵盖对应类别，仅打印提示性 `WARNING: 物料『{name}』名称疑似含致敏物『{class}』但未在过敏原中标注，请确认`（已正确标注 / 名称无关键词 / allergen 为空且名称也无关键词 均不告警），**不阻断**生成，退出码 0。

**行业专属软告警（W2/W3，非阻断）**

- **W2（电子行业）**：`industry=="电子"` 且未被排除的物料（元件清单内）未标 `rohs`（空串）→ 打印 `WARNING: 物料『{name}』未标注 RoHS 合规状态，请确认`，**不阻断**生成，退出码 0。被排除的"其他"类物料（散热片/外壳等）不校验。
- **W3（化工行业）**：`industry=="化工"` 且未被排除的物料（配方表内）未填 `cas_number` 或 `ghs_hazard` → 分别打印 `WARNING: 物料『{name}』未填写 CAS 号，请确认` / `WARNING: 物料『{name}』未填写 GHS 危险标识，请确认`，**不阻断**生成，退出码 0。被排除的"包材"类物料不校验。
- **含量(%) 列和校验（化工行业）**：所有配方原料均填了 `concentration`（非空且 > 0）时校验列和 ≈ 100%（±5%）→ 偏差超阈值打印 `WARNING: 配方表含量(%) 列和为 {total:.1f}%，偏离 100% 超过 ±5%，请确认`，**不阻断**生成，退出码 0。任一原料未填则跳过校验。

> 旧版数据（缺新字段）重新生成时会因 V1/V2/V3/V5 触发 `VALIDATION_FAILED`，需用户补全 `product_name`/`category`/`output_rate`/工序 `output`，属 R5「待补」固有结果（详见 README「已知限制」）。V4 新增字段（`industry`/7 个专属字段）均为选填，不影响阻断级校验。

校验全部通过才允许进入汇总确认。

## 汇总确认
采集完成后，输出一段结构化汇总供用户确认：
```
===== BOM 信息汇总（请确认）=====
产品名称：芒果果味糖浆
产品类别：食品
行业标识：食品
全产品出品率：130%
版本号：V1.0   生成日期：2026-07-07
【物料】（共 N 条）
 1. 名称 / 单位 / 用量 / 出品率 / ERP代码 / 物料类型 / 所属工序
 ...
【工序】（共 M 条）
 1. 编号 / 名称 / 说明 / 工时 / 备注 / 产物
 ...
【配料表预览】（仅食品类；按用量降序，含用量占比%与过敏原）
 物料名称 / 物料类型 / 计量单位 / 用量 / 出品率(%) / 用量占比% / 过敏原
 芒果果味糖浆基料 / 原料 / kg / 70.0 / 98.0% / 47.7% /
 芒果原浆 / 原料 / kg / 46.3 / 55.0% / 31.5% / 大豆,乳
 白砂糖 / 原料 / kg / 30.0 / 100.0% / 20.4% /
 柠檬酸 / 添加剂 / kg / 0.5 / 100.0% / 0.4% /
（用量占比% 经补差修正，列和恰为 100.0%；已排除非食用物料：PE 瓶 等包材/其他）
================================
确认生成？(y 生成 / 修改 重新输入 / 退出)
```
- 对 `食品` 类，额外展示「配料表预览」（仅含 原料/添加剂/香精香料，按用量降序），含「用量占比%」「过敏原」两列，并提示已排除的包材/其他/未分类物料名称。
- 对 `电子` 类，额外展示「元件清单预览」（排除"其他"类物料，按物料类型+位号排序），含「位号/型号/封装/物料名称/数量/物料类型/RoHS」并注明 V5 扩列至 14 列（含制造商/容差/额定功率/额定电压/替代料/封装温度），提示 W2 软告警（未标 RoHS 的物料名称）及已排除的"其他"类物料名称。示例：
  ```
  【元件清单预览】（仅电子类；排除"其他"类，按类型+位号排序；V5 扩列 14 列含工程字段）
   序号 / 位号 / 型号 / 封装 / 物料名称 / 数量 / 物料类型 / RoHS / 制造商 / 容差 / 额定功率 / 额定电压 / 替代料 / 封装温度
   1    / U1  / STM32F103C8T6 / LQFP48 / 主控芯片 / 1 / IC / 是 / ST / ±1% / 0.36W / 3.3V / GD32F103C8T6 / 260℃
   2    / C1  / CL10A105KP8 / 0805 / 贴片电容 / 4 / 电容 / 是 / / / / / / /
   3    / Y1  / 8MHz / HC-49S / 晶振 / 1 / 晶振 / (黄字,未标注) / / / / / / /
  （RoHS 着色：否→红字，未知/空→黄字；已排除"其他"类物料：裸PCB板、贴片完成板）
  ```
- 对 `化工` 类，额外展示「配方表预览」（排除"包材"类物料，按含量降序），含「物料名称/CAS号/含量(%)/GHS标识/物料类型/计量单位/用量」并注明 V5 扩列至 13 列（含纯度/物态/闪点/存储条件/危险等级），提示 W3 软告警（未填 CAS/GHS 的物料名称）及含量和校验结果、已排除的包材物料名称。示例：
  ```
  【配方表预览】（仅化工类；排除"包材"类，按含量降序；V5 扩列 13 列含 SDS 字段）
   序号 / 物料名称 / CAS号 / 含量(%) / GHS标识 / 物料类型 / 计量单位 / 用量 / 纯度 / 物态 / 闪点 / 存储条件 / 危险等级
   1    / 乙醇     / 64-17-5 / 65.0% / GHS02 / 主料 / kg / 6.5 / ≥95% / 液 / 13℃ / 阴凉 / 易燃液体 类别2
   2    / 去离子水 / 7732-18-5 / 30.0% /  / 溶剂 / kg / 3.0 / / 液 / / / /
  （含量(%) 列和=95.0%，偏差 ±5% 内通过；已排除包材物料：喷雾瓶）
  ```
- 对 `纺织` 类，额外展示「面料辅料清单预览」（排除"其他"类物料，按物料类型+名称升序），含「物料名称/物料类型/成分比例/纱支/克重(g/m²)/幅宽/色号」，并提示已排除的"其他"类物料名称。示例：
  ```
  【面料辅料清单预览】（仅纺织类；排除"其他"类，按类型+名称升序）
   序号 / 物料名称 / 物料类型 / 成分比例 / 纱支 / 克重(g/m²) / 幅宽 / 色号
   1    / 全棉针织布 / 面料 / 棉100% / 40S / 180 / 150cm / 藏青 01
   2    / 缝纫线 / 辅料 / 涤100% / 60S / / / 白
  （已排除"其他"类物料：裁片、OPP包装袋）
  ```
- 对 `家具` 类，额外展示「家具物料清单预览」（排除"其他"类物料，按物料类型+名称升序），含「物料名称/物料类型/材质等级/尺寸规格/表面处理/用量/色号/花色」，并提示已排除的"其他"类物料名称。示例：
  ```
  【家具物料清单预览】（仅家具类；排除"其他"类，按类型+名称升序）
   序号 / 物料名称 / 物料类型 / 材质等级 / 尺寸规格 / 表面处理 / 用量 / 色号/花色
   1    / 实木方料 / 主材 / 橡胶木 / 40×40mm / 清漆 / 2 / 原木色
   2    / 五金连接件 / 五金 / 钢 / M6 / 镀锌 / 8 / 银色
  （已排除"其他"类物料：板件、瓦楞包装纸箱）
  ```
- 对任一含 `unit_price` 物料的行业（含通用），额外展示「成本明细预览」：含「物料名称/物料类型/用量/单位/单价/币种」，并提示 `total_price` 按 `用量×单价` 实时派生（不入库）。有行业视图时编号为「四、成本明细」，否则「三、成本明细」。示例：
  ```
  【成本明细预览】（任一物料含 unit_price 即展示；有行业视图时为「四」、否则「三」）
   序号 / 物料名称 / 物料类型 / 用量 / 单位 / 单价 / 币种
   1    / 主控芯片 / IC / 1 / 个 / 12.50 / 人民币(CNY)
   2    / 贴片电容 / 电容 / 4 / 个 / 0.02 / 人民币(CNY)
  （总价 = 用量×单价，实时派生展示；成本合计见生成文件末行）
  ```
- 其它行业（通用/机械/包装）不展示行业专属视图预览；仅当含 `unit_price` 时展示上列「成本明细预览」（编号「三、成本明细」）。
- `y` → 生成 Excel
- `修改` → 进入「修改模式」：让用户指出改哪一项（物料第几条 / 工序第几条 / 版本号 / 类别 / 行业 / 出品率），回到对应采集步骤重新录入该项，然后重新汇总。
- `退出` → 按「取消与退出」处理。

版本号默认 `V1.0`，生成日期默认当天（YYYY-MM-DD）；若用户指定则采用用户给定值。

## 生成 Excel
确认后执行：
1. 将汇总数据序列化为 JSON（结构见 `references/bom-spec.md` 的「输入 JSON Schema」）。
2. 调用生成脚本（脚本会自动确保 openpyxl 可用，无需预先配置）：
   ```
   # 单文件（V6 向后兼容）
   python3 <skill_dir>/scripts/generate_bom.py --data <临时json文件> --out <输出路径.xlsx>

   # 双语（追加「BOM表(英)」工作表，主表与 V6 逐字节一致）
   python3 <skill_dir>/scripts/generate_bom.py --data <临时json文件> --out <输出路径.xlsx> --bilingual

   # B1 空白模板（默认全 8 行业；可指定 --industries 食品,电子）
   python3 <skill_dir>/scripts/generate_bom.py --blank-templates --out-dir <目录> [--industries 食品,电子] [--bilingual]

   # B2 批量生成（按目录或显式文件列表）
   python3 <skill_dir>/scripts/generate_bom.py --batch-dir <输入目录> --out-dir <输出目录> [--bilingual]
   python3 <skill_dir>/scripts/generate_bom.py --batch f1.json,f2.json --out-dir <输出目录> [--bilingual]
   ```
   说明：`<skill_dir>` 为本技能所在目录；输出路径默认当前工作目录，文件名 `BOM_<日期>.xlsx`，用户可指定。
   - **优先级**：`--blank-templates` > `--batch-dir`/`--batch` > 单文件（`--data`/`--out`）。
   - **双语提示**：当用户需要英文版 / 对外交付 / 双语对照时，加 `--bilingual`；`bilingual=False`（默认）主表与 V6 完全一致。编码类字段（`standard` 代号、CAS/GHS、行业专属代码）不翻译，仅标签翻译；「物料类型」在英文表显示英文（如 `主料`→`Main Material`）。
   - **B1**：生成 `template_<行业>.xlsx`（空白结构，不校验），适合作为录入底稿。
   - **B2**：逐文件生成 `BOM_<产品名>_<日期>.xlsx`；单文件失败不中断，结束打印「成功 N / 失败 M」，有失败则整体退出码 2。
3. 脚本打印 `OK:<路径>` 表示成功，打印 `VALIDATION_FAILED` 及错误列表表示数据问题——此时回到「数据校验」修正后重试。
4. 将生成的 .xlsx 绝对路径作为结果呈现给用户。

## 取消与退出
用户在任意阶段输入「取消 / 退出 / 不做了 / exit / quit」等中止意图时：
- 提示并提供选项：
  ```
  已取消本次录入。请选择：
  [1] 重新输入（从当前阶段该项重新开始，已采集的合法数据保留）
  [2] 退出（放弃本次 BOM 生成，不生成任何文件）
  ```
- 选 1 → 回到触发取消的那一项，重新引导录入。
- 选 2 → 终止流程，明确告知「未生成任何文件」，并提示可随时说「BOM生成」重新发起。

## 逆向流程：从 Excel 导入 BOM

适用于「已有一份由本技能生成的 BOM 表 .xlsx，想反向查看/导出其结构化数据，或在此基础上重新编辑」的场景。逆向导入也可作为「重新编辑已有 BOM」的入口：导出的 JSON 可直接重新喂给正向流程（作为 `generate_bom.py --data` 的输入）。

**触发**：用户用导入类关键词（见「触发与入口」）开启本流程，或正向流程结束后用户选择「重新编辑已有 BOM」。

**步骤**：
1. 请用户提供 .xlsx 文件路径（支持绝对/相对路径；B3 合并支持多个文件）。
2. 调用逆向解析脚本（复用与正向一致的 openpyxl 自动安装机制）：
   ```
   # 单文件 / 多文件无 --merge：按首个处理（V6 向后兼容）
   python3 <skill_dir>/scripts/import_bom.py --in <BOM.xlsx> [--out <data.json>]

   # B3 多文件合并（必填 --out）
   python3 <skill_dir>/scripts/import_bom.py --in f1.xlsx f2.xlsx ... --merge --out merged.json
   ```
   - 脚本按「一、物料信息」「二、工艺工序」标记定位，解析标题/版本号/生成日期与物料、工序数据，重建与正向输入 JSON Schema 一致的 JSON（详见 `references/bom-spec.md` 的「逆向导入」节）。
   - 脚本打印 `OK:<路径或JSON>` 表示成功；若文件非 BOM 格式或缺失必要标记，打印 `PARSE_ERROR` 并以退出码 2 结束（此时提示用户检查文件）。
   - **B3 合并**：`--in` 接受多个文件，`--merge` 将其顺序合并为单 JSON——materials/processes 按文件顺序 `extend`（不去重）；`step_no` 跨文件重复仅记 `merge_notes`（不重命名）；`industry` 取首个非空文件；顶层写入 `merged_from`（各文件 industry 列表）与 `merge_notes`（冲突/失败说明）；单文件解析失败跳过并记 `merge_notes`，不影响其余文件；全部失败则 `MERGE_FAILED`（退出码 2）。单 `--in` 无 `--merge` 维持 V6 行为（无 `merged_from`/`merge_notes`）。
3. 将解析结果以结构化汇总呈现给用户（版本号、日期、物料条数、工序条数，可附关键字段预览；B3 合并时附 `merged_from` 与 `merge_notes`）。
4. 提供选项：
   ```
   解析完成。请选择：
   [1] 重新生成 Excel（用这份 JSON 作为 --data 传回 generate_bom.py，可先编辑）
   [2] 导出 JSON 文件（--out 指定路径，便于二次处理）
   [3] 退出
   ```
   - 选 1 → 进入「正向后处理」：若用户要编辑则回到对应采集步骤修改并重新汇总，否则直接 `generate_bom.py --data <json> --out <新xlsx>` 重新生成（即闭环回写）。
   - 选 2 → 执行 `import_bom.py --in <xlsx> --out <data.json>`，把文件落盘并展示路径。
   - 选 3 → 终止流程，明确告知已解析结果不会额外生成文件（除非用户此前已选导出）。

**容错要点**：
- Excel 标题非「BOM表」仅告警不中断；找不到物料区 / 工序区标记则 `PARSE_ERROR` 退出。
- 逆向导出的 JSON 字段与正向输入 Schema 完全对齐，`usage` / `yield_rate` / `work_hours` 解析为 float（空则留空字符串），`product_name` / `erp_code` 为字符串（空则留空字符串），因此可直接回灌正向流程。

## 异常处理速查
| 情况 | 处理 |
|------|------|
| 用量 / 出品率格式非法 | 指出字段，要求重新输入该项 |
| 必填缺失 | 提示补充，停留当前项 |
| 工序编号重复 | 提示修改 |
| 用户中途取消 | 见「取消与退出」 |
| openpyxl 缺失 | 脚本自动 `pip install openpyxl`；仍失败则提示手动安装 |
| 输出路径不可写 | 改默认路径（当前目录）并重试，提示用户 |
| 输出路径已存在同名文件 | 追加序号（如 `BOM_2026-07-07_2.xlsx`）或询问是否覆盖 |
| Excel 非 BOM 格式 / 标记缺失 | 逆向解析打印 `PARSE_ERROR` 并退出；提示用户确认文件是否由本技能生成、路径是否正确 |
| 逆向解析后数据想回写 | 走正向流程：把导出的 JSON 作为 `--data` 传回 `generate_bom.py` 重新生成 Excel |

## Resources
### scripts/
- `bom_constants.py`：V4 共享常量模块（纯 Python 标准库，无第三方依赖）。定义行业枚举 `INDUSTRIES`（含 V7 `ALL_INDUSTRIES` 8 值：食品/电子/化工/机械/纺织/家具/包装/通用）、category→industry 推断映射 `CATEGORY_TO_INDUSTRY`、元件/配方过滤集 `COMPONENT_EXCLUDE`/`FORMULA_EXCLUDE`、执行标准建议 `INDUSTRY_STANDARD`，以及从 `generate_bom.py` 迁入的食品配料表过滤集 `EDIBLE` 与过敏原集合/关键词 `ALLERGEN_SET`/`ALLERGEN_HINTS`；V7 新增双语字典 `I18N`（英文键→中文值）与反向 `ZH2EN = {v: k for k, v in I18N.items()}`（仅 `generate_bom.py` 使用，`import_bom.py` 不依赖）。`generate_bom.py` 与 `import_bom.py` 均从此模块导入，保持单一真相源。
- `generate_bom.py`：读取 JSON 数据，生成 BOM 表 .xlsx（标题「BOM表」、产品名称、产品类别、全产品出品率、版本号、生成日期、物料区（8 列：含首列「序号」、ERP物料代码/物料类型/所属工序，分工序分组呈现）、工序区（含产物列）、行 5 可选展示审批人/生效日期/执行标准、物料区末「合计用量」行）；V4 按行业派生专属视图：食品→「三、配料表」（7 列，含用量占比%/过敏原），电子→「三、元件清单」（8 列，含位号/型号/封装/RoHS 红黄字标记），化工→「三、配方表」（8 列，含 CAS号/含量(%)/GHS标识）；含样式与防御性校验（V1–V7 阻断级 + V8/W1/H1/W2/W3/含量和软校验非阻断）；V7 新增 `--bilingual`（追加「BOM表(英)」工作表，主表与 V6 逐字节一致）、`--blank-templates`+`--out-dir`+`--industries`（B1 空白模板）、`--batch-dir`/`--batch`+`--out-dir`（B2 批量生成，错误隔离、失败退出码 2）；依赖 openpyxl，缺失时自装。
- `import_bom.py`：逆向导入脚本。读取由 `generate_bom.py` 生成的 BOM 表 .xlsx，**按列头文本定位列号**解析，重建与正向输入 JSON Schema 一致的结构化 JSON（含 product_name、category、industry、output_rate、erp_code、material_type、process、output、approver、effective_date、standard、materials[].allergen + 7 个专属字段，向后兼容旧版 7/5 列 Excel）；V4 从「三、」区块标记推断 industry（有元件清单→电子，有配方表→化工，有配料表→食品，无→按 category 推断）；按物料名从「三、元件清单」回收 designator/footprint/part_number/rohs，从「三、配方表」回收 cas_number/concentration/ghs_hazard；物料对象补全 7 个专属字段默认空串；V7 将 `--in` 改为 `nargs="+"` 并新增 `--merge`+`--out` 实现 B3 多文件合并（`_parse_bom_nofail` 错误隔离、`_merge_boms` 顺序 extend + step_no 冲突留痕 + `merged_from`/`merge_notes`）；CLI 为 `python3 import_bom.py --in <BOM.xlsx> [--out <data.json>]` 或 `python3 import_bom.py --in f1.xlsx f2.xlsx --merge --out merged.json`；依赖 openpyxl，缺失时自装。

### references/
- `bom-spec.md`：BOM 表 Excel 结构规范与输入 JSON Schema（含 V4 行业字段与专属视图），供生成脚本与汇总序列化参考。
- `bom-demo.svg`：BOM 表 Excel 视觉演示图（含电子元件清单/化工配方表区块）。
