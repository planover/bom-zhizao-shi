---
name: bom-zhizao-shi
description: BOM智造师 —— 交互式引导用户录入物料信息与工艺工序信息，自动完成数据校验并生成标准 BOM 表 Excel 文件；同时支持从已有 BOM 表 Excel 逆向导入为结构化 JSON。当用户提及「BOM生成」「物料清单制作」「BOM表」「做一份物料清单」「生成工艺BOM」等正向关键词，或提及「BOM导入」「从Excel导入BOM」「反向导入BOM」「导入BOM表」「BOM表导入」等逆向关键词，或希望把物料（名称/单位/用量/出品率）与工序（编号/名称/说明/工时/备注）整理成结构化 Excel / 解析已有 BOM 表时，使用此技能。
---

# BOM智造师（BOM Maker）

## 概述
本技能通过分阶段的交互式引导，采集「产品信息」「物料信息」与「工艺工序信息」，在完成数据校验与汇总确认后，调用 `scripts/generate_bom.py` 生成带表头、产品名称、物料区（含 ERP 物料代码）、工序区的 BOM 表 Excel 文件（.xlsx）。

适用场景：
- 用户说「帮我生成一份 BOM 表」「做物料清单」「BOM 生成」
- 需要把产品物料（名称 / 单位 / 用量 / 出品率 / ERP物料代码）与工序（编号 / 名称 / 说明 / 工时 / 备注）整理成标准 Excel
- 制造、工艺、采购、成本核算等需要结构化 BOM 的场景

## 触发与入口
满足以下任一即触发：

**正向（生成 BOM）**：
- 关键词命中：`BOM生成`、`物料清单制作`、`BOM表`、`物料清单`、`工艺BOM`、`生成BOM`
- 用户意图明确为「把物料和工序做成 Excel 表格」

**逆向（从 Excel 导入 BOM）**：
- 关键词命中：`BOM导入`、`从Excel导入BOM`、`反向导入BOM`、`导入BOM表`、`BOM表导入`
- 用户意图明确为「把已有 BOM 表 .xlsx 解析回结构化数据 / 重新编辑已有 BOM」

触发后，先进入「第零阶段：产品信息采集」，再进入「第一阶段：物料信息采集」，不要先做其他事。

## 执行链路总览
```
【正向】触发 → 阶段零(产品信息) → 阶段一(物料采集) → 阶段二(工序采集) → 数据校验 → 汇总确认 → 生成Excel → 输出
                 ↑ 任意阶段可取消(重新输入 / 退出)

【逆向】触发(导入类关键词) → 接收Excel路径 → 逆向解析(import_bom.py) → 展示/导出JSON → [重新生成Excel / 退出]
```
每一步的跳转条件与异常处理见后文对应小节。

## 第零阶段：产品信息采集
目标：采集产品级信息，包括产品名称（必填）、产品类别（必填）、全产品出品率（必填）、版本号与生成日期。

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 产品名称 | 产品的中 / 英文名称（如：芒果果味糖浆） | **是** | 非空（R1），留空将触发 VALIDATION_FAILED |
| 产品类别 | 产品分类 | **是** | 下拉 5 类：`食品`/`工业品`/`日化化妆品`/`医药`/`其他`（R1/R4）；仅 `食品` 触发配料表 |
| 全产品出品率 | 全产品出品率(%)（如干香菇泡发增重可 >100，通常 ≤100） | **是** | 数值 `> 0`，允许 `> 100`（无硬上限）（R2） |
| 版本号 | BOM 版本号 | 否 | 默认 `V1.0` |
| 生成日期 | BOM 生成日期 | 否 | 默认当天（YYYY-MM-DD） |
| 审批人 | BOM 审批人（可选） | 否 | 默认 `""`；非空才在表头行 5 显示 |
| 生效日期 | BOM 生效日期（可选） | 否 | 默认 `""`；非空才在表头行 5 显示 |
| 执行标准 | BOM 执行标准（可选） | 否 | 默认 `""`；食品建议填 `GB 7718-2025`；非空才在表头行 5 显示 |

引导方式：
1. 询问用户：「请输入产品名称（如：芒果果味糖浆）」（必填，不可留空）。
   - 用户输入 → 记录 `product_name`
   - 用户留空 → 提示「产品名称为必填，请补充」，停留重新输入
2. 询问产品类别：「请选择产品类别：食品 / 工业品 / 日化化妆品 / 医药 / 其他」（必填，建议以下拉呈现）。记录 `category`。
3. 询问全产品出品率：「请输入全产品出品率(%)（必填，>0，可>100，如干香菇泡发增重）」。记录 `output_rate`（数值，校验 >0）。
4. 可同时确认版本号（默认 `V1.0`）与生成日期（默认当天）；也可留到汇总确认时再确认。
5. 可选采集「审批人 / 生效日期 / 执行标准」（均为选填，留空则不显示）。三项非空时拼接显示于表头行 5（段间以空格分隔），食品类建议填执行标准 `GB 7718-2025`。
6. 采集后进入「第一阶段：物料信息采集」。

> 说明：食品类产品在汇总阶段会自动预览「配料表」并提示已排除的包材/其他类物料；非食品类不生成配料表。

## 第一阶段：物料信息采集
目标：采集至少一条物料记录（可多条，构成「物料清单」）。每条物料含 7 个字段：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 物料名称 | 物料的中文 / 英文名称 | 是 | 非空 |
| 计量单位 | 如 kg、个、米、升 | 是 | 非空 |
| 用量 | 单件产品消耗该物料的数量 | 是 | 正数（>0），数值 |
| 出品率 | 该物料产出成品的比率（%） | 是 | 正数（>0）且 ≤100，数值 |
| ERP物料代码 | 物料在 ERP 系统中的编码 | 否 | 无，可留空 |
| 物料类型 | 物料分类（用于食品配料表过滤） | 否 | 下拉：`原料`/`添加剂`/`香精香料`/`包材`/`其他`，默认 `其他` |
| 所属工序 | 该物料归属的工序（工序级流转链/分工序呈现） | 否 | 下拉引用已录入工序 `step_no`，首道工序可选填，默认空 |
| 过敏原 | 该物料的过敏原标签（仅食品类在配料表展示） | 否 | 逗号分隔，∈ 八大类+其他（含麸质谷物/甲壳类/蛋类/鱼类/花生/大豆/乳/坚果/其他）；默认空；标签非法仅 WARNING 不阻断 |

引导方式（每轮聚焦一条，降低认知负担）：
1. 输出物料录入模板，例如：
   ```
   请填写一条物料（可按格式整段回复，也可逐字段回复）：
   物料名称：
   计量单位：
   用量：
   出品率(%)：
   ERP物料代码：（可选，可留空）
   物料类型：（可选，下拉：原料/添加剂/香精香料/包材/其他）
   所属工序：（可选，引用工序编号 step_no，首道可留空）
   过敏原：（可选，逗号分隔标签：含麸质谷物/甲壳类/蛋类/鱼类/花生/大豆/乳/坚果/其他）
   ```
2. 解析用户输入，逐项校验（见「数据校验」）。
3. 校验失败时，明确指出哪个字段、何种问题，要求「重新输入该项」；校验通过后存入临时物料列表。
4. 询问「是否继续添加物料？(y/n）」。
   - `y` → 回到步骤 1 采集下一条
   - `n` → 物料数 ≥1 则进入第二阶段；若 0 条则提示「至少需要一条物料」并返回步骤 1
5. 支持批量：若用户一次性给出多条物料，按空行 / 序号拆分后逐条校验。

## 第二阶段：工艺工序采集
目标：采集零条或多条工序记录（允许无工序的纯物料 BOM）。每条工序含 6 个字段：

| 字段 | 说明 | 必填 | 校验 |
|------|------|------|------|
| 工序编号 | 如 S01、1、OP-10 | 是 | 非空，建议连续，不可重复 |
| 工序名称 | 如 切割、焊接、装配 | 是 | 非空 |
| 工序说明 | 操作要点 | 否 | 文本 |
| 工时 | 单件标准工时（可带单位，如 15min） | 否 | 若为数值须 ≥0 |
| 备注 | 其他说明 | 否 | 文本 |
| 产物(output) | 该工序产出的半成品/成品名称（工序流转链 R3 源头） | **是** | 非空；须作为下一道工序（按 step_no 升序）物料清单中的一条物料名称（R3） |

引导方式：
1. 首次进入时说明：「接下来采集工艺工序，可添加多道；全部完成后回复『结束』即可」。
2. 输出工序录入模板，工序编号可自动建议（上一条 +1）：
   ```
   工序编号（默认 S01）：
   工序名称：
   工序说明：
   工时：
   备注：
   产物(output)：（必填，该工序产物名称，需作为下道工序的物料之一）
   ```
3. 解析并校验（工时若填数值须 ≥0；必填项缺失则要求补充；编号重复则要求修改；`output` 必填非空）。
4. 多道工序时，提醒用户：下一道工序的物料清单中必须包含上一道工序的 `产物`（名称精确匹配），否则汇总/生成阶段将报「流转链不完整」（R3，阻断级）。
5. 询问「继续添加下一道工序？(y/n 或 回复『结束』）」。
   - 继续 → 回到步骤 2
   - 结束 / `n` → 进入「数据校验 → 汇总确认」
6. 允许 0 道工序：用户直接「结束」即视为纯物料 BOM（此时不触发 R3 流转链校验）。

## 数据校验
在每字段录入后即时校验，并在汇总前整体复检（校验规则 V1–V7，与 `generate_bom.py` 完全一致）：

**产品级（V1/V2/V3）**
- V1(R1)：`product_name` 必填非空 → 提示「产品名称为必填，且不可为空」。
- V2(R1)：`category` 必填且 ∈ {食品,工业品,日化化妆品,医药,其他} → 提示「产品类别为必填，且须为：食品/工业品/日化化妆品/医药/其他」。
- V3(R2)：`output_rate` 必填且 >0（允许 >100）→ 提示「全产品出品率(output_rate)为必填，且须为正数（可大于100，如干香菇泡发增重）」。

**物料级（V4/V7）**
- V4(R2)：`yield_rate` 须 0<值≤100 → 提示「物料#{i} 出品率须为 0-100 的正数」。
- 用量非数值或 ≤0 → 提示「用量必须为正数，请重新输入」。
- 物料名称/计量单位非空校验；格式异常（如用量填了文字） → 明确指出并重试。

**工序级（V5/V6）**
- V5(R3 流转链，阻断级)：仅当工序数 ≥2 时，上一工序 `output` 必填（否则提示「工序 {Sxx} 的产物(output)为必填」）；且下一道工序的物料清单（按 `process == 该工序.step_no` 过滤）必须包含上一工序 `output` 作为一条物料（名称精确匹配），否则提示「工序 {Syy} 的物料清单未包含上一工序 {Sxx} 的产物『{output}』，流转链不完整」。
- V6：工序编号唯一、名称非空、`work_hours` 若填数值须 ≥0；编号重复 → 提示「工序编号 X 已存在，请修改」。

**物料级（W1 软告警，非阻断）**
- W1：食品类物料的 `allergen` 若填写，其逗号分隔的每一标签须 ∈ 八大类+其他集合（含麸质谷物/甲壳类/蛋类/鱼类/花生/大豆/乳/坚果/其他）；出现非法标签仅打印 `WARNING: 物料『{name}』的过敏原标签『{tag}』不在八大类集合（…）内，请确认`，**不阻断**生成，可确认后继续。
- H1（名称关键词启发式，非阻断）：若物料**名称**含致敏物关键词（如 牛奶→乳、蛋→蛋类、花生→花生、大豆/黄豆→大豆、麸质/面筋→含麸质谷物、坚果→坚果、虾/蟹→甲壳类、鱼→鱼类 等）而其 `allergen` 未涵盖对应类别，仅打印提示性 `WARNING: 物料『{name}』名称疑似含致敏物『{class}』但未在过敏原中标注，请确认`（已正确标注 / 名称无关键词 / allergen 为空且名称也无关键词 均不告警），**不阻断**生成，退出码 0。

> 旧版数据（缺新字段）重新生成时会因 V1/V2/V3/V5 触发 `VALIDATION_FAILED`，需用户补全 `product_name`/`category`/`output_rate`/工序 `output`，属 R5「待补」固有结果（详见 README「已知限制」）。

校验全部通过才允许进入汇总确认。

## 汇总确认
采集完成后，输出一段结构化汇总供用户确认：
```
===== BOM 信息汇总（请确认）=====
产品名称：芒果果味糖浆
产品类别：食品
全产品出品率：130%
版本号：V1.0   生成日期：2026-07-07
【物料】（共 N 条）
 1. 名称 / 单位 / 用量 / 出品率 / ERP代码 / 物料类型 / 所属工序
 ...
【工序】（共 M 条）
 1. 编号 / 名称 / 说明 / 工时 / 备注 / 产物
 ...
【配料表预览】（仅食品类；按用量降序，含用量占比%与过敏原）
 物料名称 / 物料类型 / 计量单位 / 用量 / 出品率(%) / 用量占比% / 过敏原
 芒果果味糖浆基料 / 原料 / kg / 70.0 / 98.0% / 47.7% /
 芒果原浆 / 原料 / kg / 46.3 / 55.0% / 31.5% / 大豆,乳
 白砂糖 / 原料 / kg / 30.0 / 100.0% / 20.4% /
 柠檬酸 / 添加剂 / kg / 0.5 / 100.0% / 0.4% /
（用量占比% 经补差修正，列和恰为 100.0%；已排除非食用物料：PE 瓶 等包材/其他）
================================
确认生成？(y 生成 / 修改 重新输入 / 退出)
```
- 对 `食品` 类，额外展示「配料表预览」（仅含 原料/添加剂/香精香料，按用量降序），含「用量占比%」「过敏原」两列，并提示已排除的包材/其他/未分类物料名称。
- `y` → 生成 Excel
- `修改` → 进入「修改模式」：让用户指出改哪一项（物料第几条 / 工序第几条 / 版本号 / 类别 / 出品率），回到对应采集步骤重新录入该项，然后重新汇总。
- `退出` → 按「取消与退出」处理。

版本号默认 `V1.0`，生成日期默认当天（YYYY-MM-DD）；若用户指定则采用用户给定值。

## 生成 Excel
确认后执行：
1. 将汇总数据序列化为 JSON（结构见 `references/bom-spec.md` 的「输入 JSON Schema」）。
2. 调用生成脚本（脚本会自动确保 openpyxl 可用，无需预先配置）：
   ```
   python3 <skill_dir>/scripts/generate_bom.py --data <临时json文件> --out <输出路径.xlsx>
   ```
   说明：`<skill_dir>` 为本技能所在目录；输出路径默认当前工作目录，文件名 `BOM_<日期>.xlsx`，用户可指定。
3. 脚本打印 `OK:<路径>` 表示成功，打印 `VALIDATION_FAILED` 及错误列表表示数据问题——此时回到「数据校验」修正后重试。
4. 将生成的 .xlsx 绝对路径作为结果呈现给用户。

## 取消与退出
用户在任意阶段输入「取消 / 退出 / 不做了 / exit / quit」等中止意图时：
- 提示并提供选项：
  ```
  已取消本次录入。请选择：
  [1] 重新输入（从当前阶段该项重新开始，已采集的合法数据保留）
  [2] 退出（放弃本次 BOM 生成，不生成任何文件）
  ```
- 选 1 → 回到触发取消的那一项，重新引导录入。
- 选 2 → 终止流程，明确告知「未生成任何文件」，并提示可随时说「BOM生成」重新发起。

## 逆向流程：从 Excel 导入 BOM

适用于「已有一份由本技能生成的 BOM 表 .xlsx，想反向查看/导出其结构化数据，或在此基础上重新编辑」的场景。逆向导入也可作为「重新编辑已有 BOM」的入口：导出的 JSON 可直接重新喂给正向流程（作为 `generate_bom.py --data` 的输入）。

**触发**：用户用导入类关键词（见「触发与入口」）开启本流程，或正向流程结束后用户选择「重新编辑已有 BOM」。

**步骤**：
1. 请用户提供 .xlsx 文件路径（支持绝对/相对路径）。
2. 调用逆向解析脚本（复用与正向一致的 openpyxl 自动安装机制）：
   ```
   python3 <skill_dir>/scripts/import_bom.py --in <BOM.xlsx> [--out <data.json>]
   ```
   - 脚本按「一、物料信息」「二、工艺工序」标记定位，解析标题/版本号/生成日期与物料、工序数据，重建与正向输入 JSON Schema 一致的 JSON（详见 `references/bom-spec.md` 的「逆向导入」节）。
   - 脚本打印 `OK:<路径或JSON>` 表示成功；若文件非 BOM 格式或缺失必要标记，打印 `PARSE_ERROR` 并以退出码 2 结束（此时提示用户检查文件）。
3. 将解析结果以结构化汇总呈现给用户（版本号、日期、物料条数、工序条数，可附关键字段预览）。
4. 提供选项：
   ```
   解析完成。请选择：
   [1] 重新生成 Excel（用这份 JSON 作为 --data 传回 generate_bom.py，可先编辑）
   [2] 导出 JSON 文件（--out 指定路径，便于二次处理）
   [3] 退出
   ```
   - 选 1 → 进入「正向后处理」：若用户要编辑则回到对应采集步骤修改并重新汇总，否则直接 `generate_bom.py --data <json> --out <新xlsx>` 重新生成（即闭环回写）。
   - 选 2 → 执行 `import_bom.py --in <xlsx> --out <data.json>`，把文件落盘并展示路径。
   - 选 3 → 终止流程，明确告知已解析结果不会额外生成文件（除非用户此前已选导出）。

**容错要点**：
- Excel 标题非「BOM表」仅告警不中断；找不到物料区 / 工序区标记则 `PARSE_ERROR` 退出。
- 逆向导出的 JSON 字段与正向输入 Schema 完全对齐，`usage` / `yield_rate` / `work_hours` 解析为 float（空则留空字符串），`product_name` / `erp_code` 为字符串（空则留空字符串），因此可直接回灌正向流程。

## 异常处理速查
| 情况 | 处理 |
|------|------|
| 用量 / 出品率格式非法 | 指出字段，要求重新输入该项 |
| 必填缺失 | 提示补充，停留当前项 |
| 工序编号重复 | 提示修改 |
| 用户中途取消 | 见「取消与退出」 |
| openpyxl 缺失 | 脚本自动 `pip install openpyxl`；仍失败则提示手动安装 |
| 输出路径不可写 | 改默认路径（当前目录）并重试，提示用户 |
| 输出路径已存在同名文件 | 追加序号（如 `BOM_2026-07-07_2.xlsx`）或询问是否覆盖 |
| Excel 非 BOM 格式 / 标记缺失 | 逆向解析打印 `PARSE_ERROR` 并退出；提示用户确认文件是否由本技能生成、路径是否正确 |
| 逆向解析后数据想回写 | 走正向流程：把导出的 JSON 作为 `--data` 传回 `generate_bom.py` 重新生成 Excel |

## Resources
### scripts/
- `generate_bom.py`：读取 JSON 数据，生成 BOM 表 .xlsx（标题「BOM表」、产品名称（必填）、产品类别（必填）、全产品出品率（必填）、版本号、生成日期、物料区（8 列：含首列「序号」、ERP物料代码/物料类型/所属工序，分工序分组呈现）、工序区（含产物列）、食品类追加「三、配料表」派生区块（含用量占比%/过敏原列）、行 5 可选展示审批人/生效日期/执行标准、物料区末「合计用量」行），含样式与防御性校验（V1–V7，R3 流转链为阻断级，W1 过敏原软告警非阻断）；依赖 openpyxl，缺失时自装。
- `import_bom.py`：逆向导入脚本。读取由 `generate_bom.py` 生成的 BOM 表 .xlsx，**按列头文本定位列号**解析，重建与正向输入 JSON Schema 一致的结构化 JSON（含 product_name、category、output_rate、erp_code、material_type、process、output、approver、effective_date、standard、materials[].allergen 字段，向后兼容旧版 7/5 列 Excel）；跳过「【分组】」子标题行与「合计用量」行，按物料名称从「三、配料表」回收过敏原列，不重建配料表其他派生列；CLI 为 `python3 import_bom.py --in <BOM.xlsx> [--out <data.json>]`；文件非 BOM 格式或缺失标记时打印 `PARSE_ERROR` 并以退出码 2 结束；依赖 openpyxl，缺失时自装。

### references/
- `bom-spec.md`：BOM 表 Excel 结构规范与输入 JSON Schema，供生成脚本与汇总序列化参考。
